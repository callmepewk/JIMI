# ==========================================
# DEPENDÊNCIAS MOBILE (TERMUX SAFE)
# ==========================================

# --- Essenciais (Pure Python) ---
python-dotenv>=1.0.0
flask>=2.0.0
flask-cors>=4.0.0

# --- Core Cognitivo ---
# (Assumindo que o Ollama está rodando localmente ou em outra máquina)
ollama>=0.1.6

# ==========================================
# DEPENDÊNCIAS PESADAS (REMOVIDAS DO MOBILE)
# ==========================================
# As bibliotecas abaixo NÃO devem ser instaladas no Termux. 
# O jimi.py fará o bypass automático delas.
# - cryptography
# - requests (se não instalada via pkg)
# - google-api-python-client
# - google-auth-httplib2
# - google-auth-oauthlib
# - googlemaps
# - psutil
# - pygetwindow